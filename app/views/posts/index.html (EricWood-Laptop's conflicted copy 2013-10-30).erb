<h2>Company Blog</h2>

<!-- loop through all the available blog posts -->

<% @posts.each do |post| %>
  <article>
    <h3><%= link_to post.title, post_path(post) %></h3>
    <div>
      <%= post.content %>
    </div>
  </article>
<% end %>
